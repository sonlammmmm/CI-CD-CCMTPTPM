stages:
  - deploy

pages:
  stage: deploy
  script:
    - echo "Deploying..."
    - mkdir public
    - cp index.html public/
    - cp styles.css public/
    - mkdir -p public/js
    - cp js/main.js public/js/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"